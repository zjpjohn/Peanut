<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhn.peanut">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PeanutApplication"
        android:allowBackup="true"
        android:icon="@mipmap/dribble_launch"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".shotdetail.ShotDetailActivity"
            android:label="@string/title_activity_shot_dtail"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".profile.ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".like.LikeActivity"
            android:label="@string/title_activity_like"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".profile.MeActivity"
            android:label="@string/title_activity_me"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
